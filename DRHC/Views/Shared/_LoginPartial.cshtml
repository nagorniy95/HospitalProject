@using Microsoft.AspNetCore.Identity
@using DRHC.Models

@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager
@*if (SignInManager.IsSignedIn(User) && @Model.Admin.roll == "super")
    {
        @Html.ActionLink("Create new Admin", "Create", "Admin")
         <form asp-area="" asp-controller="Account" asp-action="Logout" method="post" id="logoutForm" class="navbar-right">

            <button type="submit" class="btn btn-link navbar-btn navbar-link">Log out</button>
        </form>


    }*@

@if (SignInManager.IsSignedIn(User))
{

    @Html.ActionLink("Logout", "Logout", "Account")

    @*<form asp-area="" asp-controller="Account" asp-action="Logout" method="post" id="logoutForm" class="navbar-right">

            <button type="submit" class="btn btn-link navbar-btn navbar-link">Log out</button>
        </form>*@

    <div class="dropdown">
        <button class="btn btn-primary dropdown-toggle" type="button" data-toggle="dropdown">
            <span class="caret"></span>
        </button>
        <ul class="dropdown-menu">
            <li>
                @Html.ActionLink("Testimonial list", "List", "Testimonial")
            </li>
            <li>
                @Html.ActionLink("Feedback List", "List", "Feedback")
            </li>
            <li>
                @Html.ActionLink("Contact List", "List", "ContactForm")
            </li>
            <li>
                @Html.ActionLink("RecRoomBooking List", "List", "RecRoomBooking")
            </li>
            <li>
                @Html.ActionLink("Add Faq ", "New", "Faq")
            </li>
            <li>
                @Html.ActionLink("Faq List", "List", "Faq")
            </li>
            <li>
                @Html.ActionLink("ErWaitTime List", "Show", "ErWaitTime")
            </li>
        </ul>
    </div>

}
else
{
    @Html.ActionLink("Log in", "Login", "Account")
    <div class="dropdown">
        <button class="btn btn-primary dropdown-toggle" type="button" data-toggle="dropdown">
            <span class="caret"></span>
        </button>
        <ul class="dropdown-menu">
            <li>
                @Html.ActionLink("Add Testimonial", "New", "Testimonial")
            </li>
            <li>
                @Html.ActionLink("Add RecRoomBooking", "Create", "RecRoomBooking")
            </li>
            <li>
                @Html.ActionLink("Show Faq", "Show", "Faq")
            </li>
            
            <li>
                @Html.ActionLink("Add Contact ", "Add", "ContactForm")
            </li>
        </ul>
    </div>


}
